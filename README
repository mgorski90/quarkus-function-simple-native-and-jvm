Simple example how to set-up classic AWS Lambda with Quarkus. Tested both with JVM (JDK 11) and native (compiled with GraalVM 20 CE).

To run native:
$ mvn clean package -DskipTests -P native
$ sam deploy -g -t sam.native.yml

To run JVM:
$ mvn clean package -DskipTests
$ sam deploy -g -t sam.jvm.yml

